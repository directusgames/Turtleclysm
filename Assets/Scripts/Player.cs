using UnityEngine;
using System.Collections;

public class Player : MonoBehaviour {

	public int m_size; // How fat our boy franklinson gets.
	public int m_depth; // Value of how deep in the oceanz.

	// Use this for initialization
	void Start () {
		m_size = 1;
		m_depth = 0;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
